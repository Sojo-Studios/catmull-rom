An implementation of the Cubic Hermite Spline using JavaScript, jQuery UI, and
the HTML5 <canvas> element.  It also doubles as quick and dirty scaffolding
to draw simple, 60fps animations to a canvas in a modern browser.